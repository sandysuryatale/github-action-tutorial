name: my first workflow
on: 
   push: 

jobs: 
   build: 
     runs-on: ubuntu-latest 

     steps: 
     -  name:  checkout code
        uses: action/checkout@v4
    
     -  name: print message 
        run: echo "hello world"
